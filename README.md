# Vending Maintenance System

Система автоматизации обслуживания вендинговых аппаратов (Vending Maintenance System) — это программный комплекс, предназначенный для управления сетью торговых автоматов, планирования технического обслуживания, контроля состояния оборудования и анализа доходности.  
Приложение состоит из клиентской части (HTML/CSS/JavaScript) и серверного API на Python Flask, взаимодействующего с Microsoft SQL Server.

## Возможности

### Управление торговыми аппаратами
- Добавление и редактирование информации об аппаратах (местоположение, модель, серийный номер, статус, доход и т.д.)
- Импорт данных из CSV-файлов с валидацией полей
- Просмотр списка аппаратов с индикацией статуса и даты следующего ТО
- Просмотр истории технического обслуживания по каждому аппарату

### Календарь обслуживания
- Отображение календаря на год с цветовой индикацией:
  - 🟢 Плановое ТО
  - 🟡 Приближающийся срок (менее 5 дней)
  - 🔴 Просроченное ТО
- Фильтрация событий по конкретному аппарату
- Всплывающие подсказки с деталями событий (модель, местоположение, описание)

### График работ
- Распределение задач по сотрудникам
- Карточки сотрудников с перечнем назначенных и выполненных ТО
- Отметка о выполненных работах

### Заявки и техническое обслуживание
- Создание заявок на обслуживание (плановое, аварийное, мониторинг)
- Ввод результатов проведённого ТО (описание, выявленные проблемы)
- Привязка заявок к аппаратам и исполнителям

### Пользовательская система
- Авторизация (сессионная / через API)
- Отображение текущего пользователя в интерфейсе
- Автоматический переход в демо-режим при потере соединения с сервером (используются локальные демо-данные)

### Импорт из CSV
- Загрузка файлов с предварительно подготовленными данными об аппаратах
- Проверка корректности структуры и типов полей
- Отображение ошибок валидации (первые 10) с возможностью их исправления

## Технологии

### Frontend
- HTML5 / CSS3 – адаптивная вёрстка
- JavaScript (ES6+) – логика интерфейса

### Backend
- Python 3.8+
- Flask – REST API
- pandas – обработка CSV и данных
- pyodbc – подключение к Microsoft SQL Server
- Flask-CORS – поддержка кросс-доменных запросов
- Werkzeug – хэширование паролей (или bcrypt)

### База данных
- Microsoft SQL Server (2016+)

## Установка и запуск

### Предварительные требования
- Python 3.8+ и pip
- Node.js 16+ и npm (только для разработки фронтенда, если используется сборка)
- Microsoft SQL Server (локальный или удалённый) с настроенным доступом
- Git

### 1. Клонирование репозитория
```bash
git clone https://github.com/Ksorarenn/franchaiserWeb.git
cd franchaiserWeb
```

### 2. Настройка базы данных
Создайте базу данных в SQL Server, например `vending_db`.  
Выполните скрипт `database.sql` (поставляется в репозитории), который создаст необходимые таблицы

Пример подключения через `sqlcmd` или SQL Server Management Studio.

### 3. Настройка бэкенда (Flask)
Перейдите в папку `API5/`:
```bash
cd API5
```

Создайте виртуальное окружение и установите зависимости:
```bash
python -m venv venv
source venv/bin/activate   # для Linux/Mac
venv\Scripts\activate      # для Windows
pip install -r requirements.txt
```

#### Файл конфигурации
Создайте файл `db.py`со следующими параметрами:
```python
# config.py
DB_DRIVER = 'ODBC Driver 17 for SQL Server'
DB_SERVER = 'localhost'          # или IP-адрес
DB_DATABASE = 'vending_db'
DB_USERNAME = 'your_username'
DB_PASSWORD = 'your_password'
```

Запустите Flask-приложение:
```bash
python app.py
```
По умолчанию сервер будет доступен по адресу `http://localhost:5000`.  
API-эндпоинты:
- `POST /api/Login` – авторизация
- `GET /api/VendingMachines` – список аппаратов
- `POST /api/vendingMachines` – добавление аппарата (в т.ч. из CSV)
- `GET /api/maintenance` – записи ТО
- `POST /api/maintenance` – создание записи ТО
- `GET /api/Users` – список пользователей
- и другие (см. документацию API)

### 4. Настройка фронтенда
Вернитесь в корень проекта. Фронтенд представляет собой статические HTML/CSS/JS файлы, которые можно открыть прямо в браузере или раздать через любой веб-сервер.

Если вы хотите изменять код, откройте файл `index.html` в редакторе.  
В файле `script.js` в начале есть конфигурация API:
```javascript
const API_CONFIG = {
    BASE_URL: 'http://localhost:5000/api/v1',  // измените при необходимости
    ...
};
```
Убедитесь, что `BASE_URL` указывает на ваш запущенный бэкенд.

Для разработки можно использовать любой локальный сервер, например, `live-server`:
```bash
npx live-server
```
или просто открыть `index.html` в браузере (но тогда API-запросы могут блокироваться CORS, если бэкенд не настроен на приём с `file://`).

### 5. Запуск в демо-режиме
Если бэкенд не запущен, приложение автоматически переключится в демо-режим, используя встроенные тестовые данные. Это позволяет оценить интерфейс без развёртывания серверной части.

## Конфигурация

### Подключение к API
Все запросы к бэкенду централизованы в объекте `ApiService` (файл `script.js`). При необходимости измените базовый URL.

### Настройка CSV-импорта
Поддерживаемые поля CSV-файла перечислены в интерфейсе загрузки. Пример заголовка:
```
Location,Model,PaymentTypeID,SerialNumber,InventoryNumber,Manufacturer,ManufactureDate,DateOfCommissioning,LastVerificationDate,VerificationInterval,ResourceHours,DateOfNextFixing,MaintenanceTimeHours,MachineStatusID,CountryID,InventoryDate,LastCheckedByUserID
```
Все поля должны соответствовать типам данных в базе.

### Настройка подключения к SQL Server
Параметры подключения задаются в `db.py` на бэкенде. Убедитесь, что используется правильный ODBC-драйвер. Список установленных драйверов можно получить командой:
```python
import pyodbc
print(pyodbc.drivers())
```

## Безопасность
- Защита от SQL-инъекций через параметризованные запросы в pyodbc
- Валидация всех входных данных на стороне клиента и сервера
- Сессионное управление аутентификацией (или JWT)
- При работе в демо-режиме все операции выполняются локально в памяти браузера, никакие данные не отправляются на сервер

## Поддерживаемые браузеры
- Google Chrome 70+
- Microsoft Edge 79+
- Safari 14+
- Firefox 65+

Требуется включенный JavaScript и поддержка `fetch` API.

## Устранение неполадок

### Ошибка подключения к базе данных
- Проверьте, запущен ли SQL Server
- Убедитесь, что в `config.py` указаны правильные учётные данные
- Проверьте, установлен ли нужный ODBC-драйвер
- Посмотрите логи Flask (в консоли) – там будет подробная информация об ошибке

### API не отвечает (CORS)
Если фронтенд открыт с `file://`, браузер может блокировать запросы к `localhost`. Используйте локальный веб-сервер (например, `npx live-server`) или настройте CORS на бэкенде (в проекте уже используется Flask-CORS с разрешением всех источников для разработки).

### CSV-файл не загружается
- Проверьте формат: разделитель – запятая, кодировка UTF-8
- Убедитесь, что все обязательные поля присутствуют
- Размер файла не должен превышать 10 МБ
- В консоли браузера могут быть видны детали ошибки

### Календарь не отображается
- Проверьте, загрузились ли данные об аппаратах и обслуживании
- В демо-режиме данные подставляются автоматически
- Убедитесь, что выбран правильный режим просмотра (год)

## Контакты
- **Репозиторий:** https://github.com/Ksorarenn/franchaiserWeb.git
- **Электронная почта:** kcondratenko@yandex.ru
- **Разработчик:** Кондратенко Константин Анатольевич, студент 3 курса специальности 09.02.07
